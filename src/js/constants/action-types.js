// Supposedly, the name of an action is always a string. This can get dicey so just save it as a constant so it can be reused universally without fear of mispelling it.
export const ADD_ARTICLE = "ADD_ARTICLE";
